<div class="row">
    <div class="col-md-4 mx-auto">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="mt-3 btn btn-primary btn-large w-100 d-block">{{ button_label|default('btn.save') }}</button>
        {{ form_end(form) }}
    </div>
</div>

